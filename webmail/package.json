{
  "name": "dpanel-webmail",
  "version": "1.0.0",
  "private": true,
  "description": "DPanel webmail — vendored from duperhuman, patched to run server-side with auto-derived IMAP/SMTP and a cookie-based 'classic webmail' fallback.",
  "workspaces": ["client", "server"],
  "scripts": {
    "dev": "concurrently \"npm run dev --workspace=server\" \"npm run dev --workspace=client\"",
    "build": "npm run build --workspace=client && npm run build --workspace=server",
    "start": "npm run start --workspace=server"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
