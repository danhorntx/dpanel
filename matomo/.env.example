# /opt/matomo/.env — runtime secrets. Generated on first deploy by
# scripts/install-matomo.sh; never committed. The values below are
# placeholders only.
MATOMO_DB_ROOT_PW=replace-with-32-char-random
MATOMO_DB_PW=replace-with-32-char-random
